import 'package:flutter/material.dart';

class SignUpCompany extends StatelessWidget {
  const SignUpCompany({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("sign company"),),
      body: null,
    );
  }
}