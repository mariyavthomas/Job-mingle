import 'package:flutter/material.dart';

class LoginCompany extends StatelessWidget {
  const LoginCompany({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Login company"),),
      body: null,
    );
  }
}